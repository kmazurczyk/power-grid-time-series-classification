# TO DO
# --- Test classes and functions --- 
# load_data.py
# eda_utils.py
# assert data paths are valid directories if os.path.exists(path), os.path.isdir(), pathlib.Path.exists()
# assert csvs exist: column_labels, event_markers
# assert expected column names in column_labels and event_markers
# --- Data tests ---
# assert expected data types
# assert expected value ranges
# - cosine angles should be between +/-180 degrees
# - typical magnitudes for high voltage lines are betweeen 110kV-500kV
# - Phases A, B, C should be, on avg, 120 degrees apart
# - IEEE typical operating frequency around 60hz
